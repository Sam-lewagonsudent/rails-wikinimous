<h2><%= @article.title %></h2><%= link_to "edit", edit_article_path(@article)%>
<p><%= @article.content %></p>


<%= link_to "back to list", articles_path(@article)%>
<%= link_to "Delete",
  article_path(@article),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
